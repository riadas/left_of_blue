function left_of(half1_arg::Half, half2_arg::Half)::Bool
    half1_arg.x < half2_arg.x
end
function left_of(half1_arg::Half, half2_arg::Half)::Bool
    half1_arg.x < half2_arg.x
end
function left_of(half1_arg::Half, half2_arg::Half)::Bool
    half1_arg.x < half2_arg.x
end
